a = (input('Введите числа через запятую, точку с запятой, слэш (1,2,3 1;2;3 1/2/3), но'
           ' только какой то один 1,2;3/4 - так нельзя: '))
b = (input('Введите числа через запятую, точку с запятой, слэш (1,2,3 1;2;3 1/2/3), но'
           ' только какой то один 1,2;3/4 - так нельзя: '))
data = []
data2 = []
for i in b:
    if i.isdigit():
        data2.append(i)
for i in a:
    if i.isdigit() and i not in b:
        data.append(i)
print(data)
