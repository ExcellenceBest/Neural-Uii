"""
List списки.

list.append(x), list.pop([i]), list.copy, list.reverse, list.insert(i,x), list.count(x)/

Dict словари.

dict.get(key), dict.items(), dict.keys(), dict.values(), dict.pop(key)

set множество.

set.add(x), set.pop(), set.copy(), set.onion(), len(x)

str  строка.

string.find(str), string.rfind(str), slice - str[start:stop:step], len(str), str.split(любой символ), string.join(список)
"""